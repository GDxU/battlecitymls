module game {
    export class tank extends spirit {
        constructor(imgKey: string,point?: point) {
            super(imgKey,point);
        }

        draw(canvas: HTMLCanvasElement) {


        }
    }
}